<% provide(:title, "Edit Profile") %>
<h1>Edit
<% if current_user!=@user && current_user.admin? %>
   <strong><%= " #{@user.username}"+
	" as #{@current_user.username}" %></strong>
<% else %>
   <%= " your Profile" %>
<% end %>
</h1>
<%= render 'form', user: @user, button_text: "Save changes" %>

